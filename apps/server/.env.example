# =
# ENVIRONMENT VARIABLES TEMPLATE
# =
# Copy this file to create your environment configuration:
#
# For local development:
#   1. Copy .env.development to .env
#   2. Update the values with your actual credentials
#
# For production (Render):
#   1. Use .env.production as reference
#   2. Set all variables in Render Dashboard (Environment tab)
#   3. DO NOT commit actual secrets to version control
# =

# Server Configuration
PORT=
HOST=
NODE_ENV=

# Frontend URL (for CORS)
CORS_ORIGIN=

# Backend URL (for OAuth callbacks and email links)
BACKEND_URL=

# Auth Secrets (REQUIRED - Generate using: openssl rand -base64 32)
# NEVER commit real secrets to version control!
AUTH_SALT=
JWT_SECRET=
SESSION_SECRET=

# Google OAuth (Optional but recommended)
# Get credentials from: https://console.cloud.google.com
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Database Configuration (PostgreSQL)
DATABASE_ENABLED=
DATABASE_URL=

# Redis Configuration (Optional but recommended for production)
REDIS_ENABLED=
REDIS_URL=

# Game Settings
TURN_TIME_LIMIT=
MAX_PLAYERS_PER_ROOM=
ROOM_DISPOSE_TIMEOUT=
RECONNECT_TIMEOUT=

# Logging
LOG_LEVEL=
LOG_PRETTY=
